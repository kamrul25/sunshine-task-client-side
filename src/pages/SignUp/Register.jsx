import { Helmet } from "react-helmet-async";

const Register = () => {
    return (
        <div>
            <Helmet>
                <title>Job Portal | Register</title>
            </Helmet>
        </div>
    );
};

export default Register;