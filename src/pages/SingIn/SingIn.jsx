import { Helmet } from "react-helmet-async";

const SingIn = () => {
    return (
        <div>
            <Helmet>
                <title>Job Portal | Sign In</title>
            </Helmet>
        </div>
    );
};

export default SingIn;